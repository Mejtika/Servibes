export interface CheckoutRequest {
  price: number;
}
export interface AppointmentDto {
  appointmentId: string;
  clientName: string;
  employeeName: string;
  serviceName: string;
  price: number;
  start: string;
  end: string;
}
